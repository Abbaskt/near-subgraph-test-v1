{
  "name": "near-subgraph-test-v1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ abbaskt/near-subgraph-test-v1",
    "create-local": "graph create --node http://localhost:8020/ abbaskt/near-subgraph-test-v1",
    "remove-local": "graph remove --node http://localhost:8020/ abbaskt/near-subgraph-test-v1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 abbaskt/near-subgraph-test-v1",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.35.0",
    "@graphprotocol/graph-ts": "0.28.1"
  }
}
