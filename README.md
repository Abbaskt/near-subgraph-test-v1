# Near BlockHandler subgraph

## Steps to run

* Download required modules
```shell
npm install
```
* To generate the `./generated` folder containing the schema
```shell
graph codegen
```
* Building the project
```shell
npm run build
```
* Deploying the project
```shell
npm run deploy
```